import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:interLibras/app/modules/interpreter/interpreter_controller.dart';
import 'package:interLibras/app/modules/interpreter/interpreter_module.dart';

void main() {
  initModule(InterpreterModule());
  // InterpreterController interpreter;
  //
  setUp(() {
    //     interpreter = InterpreterModule.to.get<InterpreterController>();
  });

  group('InterpreterController Test', () {
    //   test("First Test", () {
    //     expect(interpreter, isInstanceOf<InterpreterController>());
    //   });

    //   test("Set Value", () {
    //     expect(interpreter.value, equals(0));
    //     interpreter.increment();
    //     expect(interpreter.value, equals(1));
    //   });
  });
}
