import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:interLibras/app/modules/interpreter/interpreter_page.dart';

void main() {
  testWidgets('InterpreterPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(InterpreterPage(title: 'Interpreter')));
    //  final titleFinder = find.text('Interpreter');
    //  expect(titleFinder, findsOneWidget);
  });
}
